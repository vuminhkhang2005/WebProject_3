services:
  - type: web
    name: survey-app
    env: docker           # BẮT BUỘC: docker
    plan: free
    branch: main          # hoặc branch repo của bạn
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    healthCheckPath: /
